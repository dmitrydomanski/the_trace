import React from 'react'
import classes from './CardFooterOverlay.css'

const cardFooterOverlay = (props) => (
        <div className={classes.CardFooterOverlay}>Overlay</div>
)

export default cardFooterOverlay